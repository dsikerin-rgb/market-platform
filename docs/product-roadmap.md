# Product Roadmap / статус работ

**Market Platform** — цифровая система управления рынком: управляющая компания, арендаторы, торговые места, договоры, начисления/оплаты, документы, заявки, уведомления, интеграции и карта рынка.

Документ фиксирует текущее состояние продукта, уже закрытые блоки, ближайшие проверки и следующие задачи. Обновлять после крупных UX/API/DB-изменений, импортов 1С и после закрытия значимых сценариев.

Дата актуализации: 2026-06-09
Актуальный prod-контур после последнего блока: `8596132`

---

## 1. Текущее состояние по крупным блокам

### 1.1. Карта рынка, ревизия и исторические группы мест

**Статус:** рабочий контур есть; текущий цикл по историческим группам и разделению parent-фигур закрыт на prod.

Что уже есть:

- страница результатов ревизии;
- вкладки:
  - `Ревизионные решения`;
  - `Связь не подтверждена`;
  - `Дубли арендаторов`;
  - `Применено`;
- базовые счётчики проверенных и оставшихся мест;
- отображение конфликтов, безопасных изменений и совпавших результатов;
- логика отображения карты учитывает разные уровни уверенности связи:
  - exact link по торговому месту;
  - tenant fallback, когда точной связи с местом нет;
  - неактивные/свободные места не должны краситься долгом арендатора;
- карта и карточки различают:
  - долг по конкретному месту;
  - общий долг арендатора;
  - нейтральный статус совместного использования, когда точная финансовая связь с конкретным местом не подтверждена;
- для исторических составных карточек добавлен безопасный сценарий `Закрыть как историческое составное место`:
  - закрывает ревизионный конфликт в одном окне;
  - не переводит место в текущую группу;
  - не добавляет текущие физические части в состав группы;
  - не переносит начисления, договоры, долги, кабинет и товары;
  - фиксирует решение обязательным комментарием ревизора.
- добавлена явная модель эпизодов групп мест:
  - список `Эпизоды групп мест` доступен super-admin через диагностику/ревизию;
  - текущие группы backfill-ятся в episodes;
  - история договора использует episodes для состава группы на дату договора;
  - текущие child-места не шумят во вкладке неподтверждённых финансовых связей, если их связь должна проверяться через parent;
- на карте добавлен режим редактирования фигур:
  - split по вертикали/горизонтали;
  - группировка/разгруппировка;
  - отмена выбора;
  - split parent-фигуры в существующие места без фигур;
  - split не требует ручных ID и не перезаписывает площади существующих мест.

Закрытый контрольный кейс на prod:

- `ОС8 6, 7` разобрано как историческая составная карточка;
- текущие места `ОС8 6` и `ОС8 7` остаются отдельными текущими местами;
- финансовая история и начисления остались на исторической карточке;
- карточка ушла из списка `Нужно уточнить`.
- `СТ2-4` разделено на существующие места `СТ 2` и `СТ 3`:
  - `СТ2-4` осталось историческим parent-местом;
  - новые фигуры привязаны к существующим местам без фигур;
  - неправильные имена вида `СТ2-4-1`/`СТ2-4-2` не создавались;
  - площади `СТ 2`/`СТ 3` не перезаписывались split-операцией.

Связанные PR:

- `#789` — backend-case `historical_group_structure` и decision `historical_composed_space_reviewed`;
- `#790` — UI-кнопка `Закрыть как историческое составное место`;
- `#791` — эта кнопка доступна и для ручных duplicate-кейсов без кандидата.
- `#848` — нейтральное объяснение статуса совместного использования на карте;
- `#853` — классификация причин неподтверждённых финансовых связей;
- `#854`/`#855` — упрощение действий и компоновки карточек неподтверждённых финансовых связей;
- `#857` — скрытие нормальных child-связей из неподтверждённых финансовых связей;
- `#858` — фиксация политики форм групп/child/parent на карте;
- `#859`/`#860` — read-model и backfill эпизодов групп мест;
- `#864` — использование эпизодов групп в истории договоров;
- `#865`–`#874` — инструменты split на карте и доведение сценария `СТ2-4 → СТ 2 + СТ 3`.

Следующий приоритетный продуктовый блок:

- перейти от разовых map-операций к устойчивому daily-flow:
  - безопасно фиксировать освобождение/занятие места;
  - менять текущего арендатора без подмены 1С-истории;
  - вести понятную историю локальных операций;
  - не ломать уже заведённые episodes и договорную историю.

---

### 1.2. Дубли арендаторов

**Статус:** рабочий MVP закрыт на prod.

Результат:

- дубли арендаторов выводятся во вкладке `Дубли арендаторов` человеческим языком;
- технические детали 1С скрыты в раскрываемом блоке `Технические данные 1С`;
- `Score`, `external_id`, `one_c_uid`, `dry-run`, `preflight`, `tenants:merge` убраны из основного пользовательского UI;
- есть два основных сценария:
  - если это один арендатор — `Подготовить слияние`;
  - если это разные арендаторы — `Это разные арендаторы`;
- скрытые пары видны в блоке `Скрытые пары`;
- ошибочно скрытую пару можно вернуть кнопкой `Вернуть в список`;
- после слияния дубль исчезает из активного списка;
- после нового импорта 1С проверено, что ранее слитая пара `#299 → #390` не вернулась как активный дубль;
- пользователь разобрал активные дубли на prod.

Основные PR/коммиты:

- `1b33761` — UI-сценарий слияния дублей из админки;
- `f50786b`, `e5a2861`, `fc4f11d` — уточнение текста подтверждений и автообновление списка;
- `763467e` — приведение экрана дублей к человеческому UX;
- `56f4c3a` — упрощение модалки слияния дубля;
- `5af9462` — возможность скрыть ложный дубль (`Это разные арендаторы`), миграция `tenant_duplicate_ignores`;
- `cb405a1` — блок `Скрытые пары` и `Вернуть в список`.

Таблицы/данные:

- `tenant_external_aliases` — защищает будущие импорты 1С от повторного создания уже слитого арендатора;
- `tenant_duplicate_ignores` — хранит пары, вручную отмеченные как разные арендаторы.

Проверочный сценарий после каждого импорта 1С:

1. Открыть `Результаты ревизии → Дубли арендаторов`.
2. Проверить, что ранее слитые пары не вернулись в активный список.
3. Проверить, что скрытые пары остались только в `Скрытые пары`.
4. Если появились новые активные пары — разобрать их по одной:
   - реальный дубль → `Подготовить слияние`;
   - разные арендаторы → `Это разные арендаторы`.

Отложенные улучшения:

- история слияний на карточке арендатора;
- краткая сводка по каждой карточке дубля: договоры, начисления, торговые места, пользователи;
- красивое форматирование даты в `Скрытые пары`.

---

### 1.3. Слияние арендаторов

**Статус:** рабочий сценарий есть, UI доведён до понятного MVP.

Что сделано:

- super-admin может подготовить слияние из UI;
- перед слиянием выполняется проверка, что будет перенесено;
- пользователь видит понятные роли:
  - `Основная карточка`;
  - `Карточка-дубль`;
- пользователь может поменять основную карточку;
- после проверки показывается блок `Будет перенесено`;
- перед фактическим слиянием есть подтверждение;
- перед слиянием система создаёт backup;
- если backup не создан — слияние не должно начинаться;
- после слияния список дублей обновляется автоматически;
- будущий импорт 1С использует alias и не создаёт повторно уже слитого арендатора.

Важно:

- backend-команда `tenants:merge` остаётся базовым безопасным механизмом;
- UI скрывает технические детали от пользователя, но не отменяет внутренние проверки;
- prod/staging миграции и любые DB-изменения выполняются только через контроль-барьер и backup.

---

### 1.4. Импорт 1С

**Статус:** рабочий импорт есть, нужен регулярный контроль качества после загрузок.

Что важно контролировать после каждого импорта:

- не восстановились ли уже слитые арендаторы отдельными карточками;
- не появились ли новые дубли арендаторов;
- не появились ли новые неподтверждённые связи торговых мест;
- не ухудшились ли статусы карты;
- корректно ли сработали alias из `tenant_external_aliases`.

Ближайший контроль:

- после следующего импорта открыть `Дубли арендаторов` и проверить активные пары;
- затем перейти к вкладке `Связь не подтверждена`.

---

## 2. Что сделано за последний цикл

### 2.1. UX дублей арендаторов

Было:

- технический язык в UI: `Сигналы качества данных`, `Score`, `external_id`, `one_c_uid`, `dry-run/preflight`, `tenants:merge`;
- пользователю было непонятно, что делать дальше;
- ложные дубли нельзя было нормально закрыть.

Стало:

- вкладка называется `Дубли арендаторов`;
- заголовок: `Возможные дубли арендаторов`;
- текст объясняет задачу простым языком;
- технические поля спрятаны;
- есть понятные действия:
  - `Подготовить слияние`;
  - `Это разные арендаторы`;
  - `Вернуть в список` для скрытых пар.

### 2.2. Безопасное слияние из UI

Сделано:

- модалка с понятным процессом;
- проверка перед слиянием;
- подтверждение перед фактическим слиянием;
- backup перед слиянием;
- автообновление списка после успешного merge.

### 2.3. Скрытие ложных дублей

Сделано:

- новая таблица `tenant_duplicate_ignores`;
- endpoint `POST /admin/tenant-duplicates/ignore`;
- кнопка `Это разные арендаторы`;
- активный список дублей больше не показывает скрытые пары;
- endpoint `POST /admin/tenant-duplicates/restore`;
- блок `Скрытые пары` внутри вкладки `Дубли арендаторов`;
- кнопка `Вернуть в список`.

### 2.4. Исторические составные места в ревизии

Сделано:

- добавлен backend-case `historical_group_structure`;
- добавлен decision `historical_composed_space_reviewed`;
- добавлена кнопка `Закрыть как историческое составное место`;
- для ручных duplicate-кейсов без кандидата показывается альтернатива: закрыть как историческое составное место или разобрать дубль;
- закрытие требует комментарий;
- закрытие не меняет группировку, не переносит начисления и не создаёт текущую группу.

Проверено на prod:

- `ОС8 6, 7` закрыто как историческая составная карточка;
- карточка пропала из списка `Нужно уточнить`;
- финансовая история осталась на исторической карточке.

---

## 3. Ближайшие задачи по приоритету

### P0 — контроль после следующего импорта 1С

Цель: убедиться, что очищенное состояние не ломается новым импортом.

Проверить:

- активные дубли не вернулись;
- скрытые пары остались скрытыми;
- слитые арендаторы не появились снова отдельными карточками;
- новые пары, если появились, действительно новые.

Результат проверки фиксировать в `docs/ops-log.md`.

---

### P1 — вкладка `Связь не подтверждена`

Цель: сделать такой же рабочий цикл, как для дублей арендаторов.

Текущая проблема:

- вкладка показывает спорные связи, но пользователю нужен понятный сценарий закрытия проблемы.

Ожидаемый сценарий:

1. Пользователь видит место и предполагаемую связь.
2. Если связь верная — подтверждает.
3. Если связь неверная — отклоняет/скрывает.
4. Если ошибся — возвращает в проверку.

Предварительные задачи:

- посмотреть текущий UI карточек `Связь не подтверждена`;
- определить, каких данных не хватает для решения;
- спроектировать минимальные действия без перегруза.

---

### P2 — сводка по карточкам дублей

Цель: уменьшить ручные переходы в карточки арендаторов.

Идея:

- на карточке дубля показывать краткую сводку:
  - договоры;
  - начисления;
  - торговые места;
  - пользователи.

Польза:

- проще выбрать основную карточку;
- меньше риск слияния не туда;
- быстрее ручная проверка.

Статус: отложено, не блокирует текущий процесс.

---

### P3 — история слияний арендаторов

Цель: дать пользовательский аудит действий.

Идея:

- на карточке арендатора показывать блок `История слияний`:
  - кто был слит;
  - в кого слит;
  - кто выполнил;
  - когда;
  - был ли создан backup.

Статус: отложено. Сейчас есть технические следы: лог, backup, alias, но нет удобного пользовательского отображения.

---

### Новые кандидаты в дорожную карту

Список зафиксирован как входящий backlog. Перед реализацией каждый пункт нужно превратить в отдельную задачу с ожидаемым результатом, владельцем данных, UX-сценарием и проверкой на prod/staging.

#### Финансы, ОСВ и 1С

- ОСВ на страницу арендатора.
- ОСВ на страницу места.
- Добавить оплаты / ОСВ в кабинет арендатора.
- Получение ОСВ из 1С по расписанию.
- Настроить работу с почтой.

#### Арендаторы и коммерческий контур

- Страница потенциальных арендаторов.
- Создать рейтинг арендаторов.
- API Instagram, VK.

#### Коммуникации

- Доработать мессенджер внутри сервиса.
- ИТ-чат / консультант.

#### Администрирование, безопасность и диагностика

- Аудит Permissions.
- Страница автодиагностики сервиса.
- Перенос проекта на сервер заказчика.
- Аудит дашборда.

#### UX и справочники

- Адаптация страниц под экран по ширине.
- Разобраться с типами мест на странице мест.

---

## 4. Правила deploy и БД

### 4.1. Общие правила

Запрещено на staging/prod:

- `php artisan migrate:fresh`;
- `php artisan migrate:refresh`;
- `php artisan db:wipe`;
- любые варианты `fresh`, `refresh`, `wipe`;
- destructive-команды с `--seed`.

Разрешённая миграция на staging/prod:

```bash
php artisan migrate --force
```

Только после контроль-барьера и backup.

### 4.2. Контроль-барьер перед миграциями/DB-экспериментами

Перед любым действием, которое может тронуть БД на staging/prod:

1. Показать Laravel DB config без секретов:
   - connection;
   - driver;
   - host;
   - port;
   - database;
   - username.
2. Показать размер PostgreSQL-БД.
3. Сделать backup через `pg_dump`.
4. Убедиться, что backup-файл существует и размер больше нуля.
5. Только потом запускать `php artisan migrate --force`.

### 4.3. Последние важные backup-файлы

Staging перед `tenant_duplicate_ignores`:

```text
storage/backups/market_staging_before_tenant_duplicate_ignores_20260524_093029.dump
```

Prod перед `tenant_duplicate_ignores`:

```text
storage/backups/market_prod_before_tenant_duplicate_ignores_20260524_094029.dump
```

---

## 5. Текущий фокус

Сейчас блок `Дубли арендаторов` закрыт как рабочий MVP, а для ревизии добавлен безопасный сценарий исторических составных мест.

Ближайший продуктовый фокус:

1. Дождаться следующего импорта 1С.
2. Проверить, что дубли не вернулись.
3. После этого переходить к вкладке `Связь не подтверждена`.

Если импорт покажет новые активные дубли — сначала разбираем их, затем продолжаем работу над связями торговых мест.

---

## 6. Временные группы и учётный фонд по периодам

Полное нормативное описание см.: [Проявленность временных group-карточек](market-space-period-effectiveness.md)

### Этап 1 — Computed effectiveness MVP

- создать `MarketSpacePeriodEffectivenessService`;
- подключить его к dashboard/statistics;
- исключить задвоение parent + child;
- добавить regression tests;
- без миграций.

### Этап 1.1 — Review safety для исторических составных мест

Реализовано:

- `Закрыть как историческое составное место` для ревизии;
- сценарий закрывает конфликт без переноса связей;
- сценарий не создаёт текущую группу;
- сценарий не заменяет explicit group episodes.

### Этап 2 — Missing shape logic

- ordinary active без shape считать проблемой;
- parent/group без shape не считать проблемой;
- inactive без shape не считать проблемой.

### Этап 3 — Review safety

- не создавать ревизионный конфликт только из-за отсутствия shape у group;
- для group/duplicate ambiguity не делать tenant-switch primary;
- добавить сценарии `проверить состав группы` / `разобрать структуру`.

### Этап 4 — Group episodes

- добавить явные периоды проявленности group;
- добавить историю состава episode;
- переключить `MarketSpacePeriodEffectivenessService` с computed-источников на explicit episodes.
