# Аудит площадей дашборда, точка остановки 2026-06-19

## Состояние после последних правок

Источник факта: prod БД, рынок `1`.

Последние примененные изменения:

- `75 - 2/О Офис 2 этаж`: фигура `379` перенесена с неактивного дубля `378` на активное место `75`. Операция `371`.
- `360 - Курилка`: площадь установлена `5,5 м2` по строке `Курилка УК` в июньской таблице. Операция `372`.
- `81 - Офис 4` и `82 - П2-30` не трогать: оставлены как служебные места, спор с таблицей директора вынесен отдельно.

Перед правками был сделан prod backup:

- `storage/backups/market_before_75_shape_360_kurilka_20260619_155546.dump`

Текущие итоги дашборда после правок:

| Срез | Всего | Сдано | Свободно | Служебное |
| --- | ---: | ---: | ---: | ---: |
| Улица | 375,0 | 210,0 | 165,0 | 0,0 |
| Без улицы | 8606,2 | 7080,4 | 961,9 | 563,9 |
| Итого | 8981,2 | 7290,4 | 1126,9 | 563,9 |

## 8 мест без фигуры

Это активные места, которым по правилам карты нужна собственная usable-фигура, но сейчас ее нет. Pending = 0, потому что у всех уже стоит статус ревизии; поэтому интерфейс мог показывать, что непройденных мест без фигур нет. Для дашборда это все равно проблема: без фигуры место не попадает в расчет площади.

| ID | Место | Площадь | Статус БД | Арендатор | Ревизия | Краткий вывод |
| ---: | --- | ---: | --- | --- | --- | --- |
| 3 | П3/2 | 30,0 | occupied | Губарев А.Н. | conflict | В таблице есть строка `П3/2` на 30,0 м2. Похоже на реальное место, но была заметка о неправильной привязке. Нужна ручная проверка фигуры рядом с `П3/2/склад`. |
| 9 | П/5 | 10,0 | occupied | Филоненко Сергей Иванович ИП | matched | В таблице есть строка `П/5` на 10,0 м2. Похоже на реальное место без фигуры; рядом есть `П/5/1` на 123,2 м2 с фигурой. |
| 68 | ФК 8/1 | 25,0 | occupied | Косачев Евгений Сергеевич ИП | matched | Подозрительно: в таблице явно найдена строка `Фк 8/1` как склад на 13,0 м2, которая соответствует месту `67` с фигурой. Перед правкой проверить, не дубль ли `68`. |
| 122 | СТ Телега | 4,0 | occupied | Москвин Алексей Прокопьевич | matched | В таблице `ст телега` стоит как свободная площадь 4,0 м2, а в БД место занято. Нужно проверить договоры/начисления по Москвину перед изменением статуса. |
| 133 | ОС1 13 | 20,0 | vacant | - | matched | Похоже на старую или дублирующую карточку: рядом есть `232 - ОС1 13` как child с фигурой. Не добавлять в дашборд без проверки, можно задвоить площадь. |
| 222 | ОС1 7, 8 | 20,0 | vacant | - | matched | По истории операций отмечено как legacy/дубль к групповой структуре `ОС1 7,8,У`. Вероятно, не должно получать отдельную фигуру. |
| 168 | ОС8 6, 7 | 20,0 | vacant | - | matched | По истории операций отмечено как историческая составная карточка. Вероятно, не должно получать отдельную фигуру. |
| 226 | ОС8 8, 9, 10 | 30,0 | vacant | - | matched | По истории операций отмечено как историческая дублирующая карточка к основному групповому месту `169 - ОС8 8, 9, 10`. Вероятно, не должно получать отдельную фигуру. |

## Следующая точка входа

Не продолжать арифметическими автоправками. Дальше разбирать только эти 8 мест:

1. `П3/2` и `П/5`: найти или нарисовать корректные фигуры, если физически есть отдельные места.
2. `ФК 8/1`: сначала подтвердить, не дубль ли это к `67 - Фк 8/1` на 13,0 м2.
3. `СТ Телега`: сначала проверить договоры и движения по Москвину; таблица считает место свободным.
4. `ОС1/ОС8` legacy-места: скорее выводить из оперативной части или менять правило, чтобы они не требовали фигуру и не влияли на дашборд.
